services:
  - type: web
    name: agency-machine-web
    env: python
    plan: free
    region: oregon
    buildCommand: pip install -r requirements.render.txt
    startCommand: gunicorn -b 0.0.0.0:$PORT dashboard:app
